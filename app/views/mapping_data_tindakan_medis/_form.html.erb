<%= form_for(@mapping_data_tindakan_medi) do |f| %>
  <% if @mapping_data_tindakan_medi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mapping_data_tindakan_medi.errors.count, "error") %> prohibited this mapping_data_tindakan_medi from being saved:</h2>

      <ul>
      <% @mapping_data_tindakan_medi.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
    <%= f.label :kode_rs, :class => "required" %><br />
    <%= f.text_field :kode_rs, :class => "required" %>
  </div>
  <div class="field">
    <%= f.label :kode_ejkbm, :class => "required" %><br />
    <%= f.collection_select :kode_ejkbm, TindakanMedi.order("kode"), :kode, :nama_kategori, {:prompt => true}, :class => "required" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
